import 'package:flutter/material.dart';

class MultidayEventEdit extends StatelessWidget {
  const MultidayEventEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
